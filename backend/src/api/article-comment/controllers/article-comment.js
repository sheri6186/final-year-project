'use strict';

/**
 * article-comment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::article-comment.article-comment');
